# Only submit.php is meant to be called from the browser.
<FilesMatch "^(config|config\.local|config\.local\.sample|lib)\.php$">
  <IfModule mod_authz_core.c>
    Require all denied
  </IfModule>
  <IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
  </IfModule>
</FilesMatch>
